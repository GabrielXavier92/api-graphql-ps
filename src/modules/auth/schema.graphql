# import Message from "../shared.graphql"

type Query {
	me: String
}

type Mutation {
	register(email: String!, name: String!, password: String!): [Message!]
	login(email: String!, password: String!): [Message!]
	forgotPassword(email: String!): [Message!]
}
